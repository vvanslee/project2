CREATE DATABASE IF NOT EXISTS kubo_db;
USE kubo_db;

CREATE TABLE tblWorktickets (
    WorkticketID INT AUTO_INCREMENT NOT NULL,
    UserID INT NOT NULL,
    Subject VARCHAR(255),
    Message VARCHAR(5000),
    Status VARCHAR(255),
    Date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (WorkticketID)
);